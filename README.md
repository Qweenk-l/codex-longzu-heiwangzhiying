# 龙族：黑王之影 · 第一季剧情测试

独立 Vue 3 + TypeScript + Vite 程序。已接入9月6日四册修订稿和序章至第十三章，476节点／251存储选项；74项程序测试、类型检查／构建及12项浏览器测试通过，待用户体验验收。C16剧情连续性与后续取证机会已实装，旧全季进度及季终归档可升级，见项目总进度表。

最新阶段、待确认事项及每次增删改统一查看[项目总进度表](../04-开发规划/项目总进度表.md)。每次工作开始先读总表，交付前同步实际变化；本 README 主要保留启动与程序结构说明。

章节测试通过“打开章节列表”进入右侧抽屉。章节条目来自实际运行包；序章至第十三章全部可独立测试。列表独立滚动，可通过关闭按钮、Esc或遮罩关闭。

阅读按“当前合并页 + 选项”呈现：选择后，上一选择的结果、自动承接和下一场正文一起成为新页，从页首阅读，不加“继续”按钮。顶部“剧情记录”可只读回看完整旧页，与检查点回退分开；主阅读/行动区保持70% / 30%。本次规则直接更新在主程序设计方案v0.2中。

## 启动

依赖已安装时，可双击本目录的 `启动试玩.cmd`，终端与浏览器会打开；关闭终端即停止本地服务。若已经在运行，直接访问下列地址即可，不必再启动一份。

需要 Node.js 24.0 及以上的 24.x 版本（本次使用 24.19.0）。在此目录执行：

```powershell
npm.cmd install
npm.cmd run dev -- --port 5173 --strictPort
```

然后打开 http://127.0.0.1:5173 。使用同一浏览器、同一地址续读，换浏览器或地址请先导出存档。

## 验证

```powershell
npm.cmd test
npm.cmd run type-check
npm.cmd run build
npm.cmd run test:e2e
```

浏览器测试默认使用本机 Microsoft Edge。测试不访问真实玩家存档。

## 结构与资料

- `src/content/stage-one.json` 是程序运行副本；`content-source` 保留本次生成依据，`scripts/build-content.mjs` 可复现生成。
- `src/engine` 独立处理条件、选择、自动段落、检查点及章节前情。
- `src/storage` 使用 IndexedDB；导入会重放合法选择校验全部剧情状态。
- `src/components` 和 `src/style.css` 负责阅读呈现；70/30 双区为首版固定约定。正文、安全加粗渲染与玩法状态分离，后续美术及视听功能可以在表现层增加。
- AI、其他可玩角色、音乐和正式美术尚未实装。当前输入只匹配可见的脚本选项。

内容真值仍为上一级项目的当前参考稿；序章至第二章以 v0.4B 为准，第三至第五章以 v0.5D 为准；三处早期修正和本轮第五章两处获批修正仅应用运行副本。详情见 `docs/content-mapping.md`、`docs/第三至第五章内容接入映射.md` 和上一级的接入审计。

自动存档受当前浏览器存储条件影响。清除网站数据会删除本地进度；导出文件可在另一浏览器通过导入恢复。当前运行包 `v0.7C-season-one.20260907.1` 支持本程序 `v0.4B-stage-one.1` 至 `.5` 的已知存档升级：先严格校验旧会话，再重放到新版本；原第二章终点存档会接续第三章；原v0.5D-stage-two.1／.2第五章终点存档会接续第六章；不兼容旧游戏存档或任意剧情版本。

2026-09-05：CH3-03采用用户确认的新蹭饭对白，现有v0.5D Markdown／Word原位标注并同步运行副本；上一运行版 `v0.5D-stage-two.1` 的旧正文存档可精确升级。

第六至第九章已按v0.7C接入，原稿未修改；两处获批修正只进入运行副本。详见[接入映射](docs/第六至第九章内容接入映射.md)。

## 9月6日修订与全季接入

内容来源为项目审阅目录中9月6日四册修订稿，程序只读取工程内快照。原稿与批注保留，运行正文排除审阅标记；具体实施与验证见[本轮计划](docs/2026-09-07-全季接入实施计划.md)。

旧版存档先按封存的旧运行包校验，再以实际选择重放至新稿。第一季正式结束后，各已抵达结果分别保留最近一次归档；回退或重新开始不删除归档。目录可载入已达结局，导出包含当前周目和归档；章节测试不写入正式归档。完整验证通过后，再交用户体验验收。


C16修订仅作用运行副本，9月6日源稿保留。先交枪后可以通过有依据的非致命提案重新证明信任；正式取证申请可取得经核验的护兄影像，救援仍需要既定证据、准备与代价。旧档重放按新规则计算，部分旧路线结局可能改变，归档每类保留较后记录。详情见现有[接入映射C16](docs/第十至第十三章内容接入映射.md#c16-全季剧情自审修订)与总进度表。
