# 第三至第五章内容接入映射

日期：2026-09-05。用户本轮已授权接入第三至第五章；参考稿内历史“暂不实装”状态不构成本轮阻止条件。序章至第二章用户验收状态由项目唯一总进度表维护。

## 来源与范围

- 原稿：`01-剧情文档/01-当前参考稿/龙族试玩第三至第五章剧情与玩法设计稿-v0.5D-衔接修订标注版.md`。
- 程序快照：`content-source/v0.5D.md`；逐字节复制。初次接入时原稿只读；随后按用户授权原位修订CH3-03，现有Markdown／Word与快照已同步。当前Markdown与快照 SHA-256 均为 `300e3a2de00b3f00b785ec47599b6c6035e1f6bc869f325ae45c79a5f5971eb4`。
- `scripts/build-chapters-three-five.mjs` 只读取程序本地快照；JSON 基线仅核对节点与选择 ID，不用于替代原稿正文、选项、效果或转移。
- 34 个主节点、54 个短结果节点、6 个共同结果节点，共 94 个节点。原稿共有 19 个选择节点、57 个选项；两处条件措辞各拆出一个存储版本，最终 59 个存储选项，每次只显示互斥版本之一。
- 第三、第四章末分别保留 `CP-CH3-END`、`CP-CH4-END` 并自动承接；第五章 `CH5-14` 保留 `CP-CH5-END`，本轮为 `stageEnd`，没有第六章正文和第一季完成状态。

## 提取规则

每个“玩家可见正文”和“共同结果”独立生成静态正文。共同结果保留原稿 ID，不提前拼入选择前正文；短结果保留原稿 ID，并按原稿转移表自动合流，无额外继续按钮。

去除系统处理、写入说明、时钟说明、共同后继和建议延时等元数据。短结果末尾“自动进入”路由说明从正文中剥离，前面的反馈正文完整保留。CH5-04 删除线旧句不进入运行包；采纳的红字段落保留，去掉修订标签与引用符号。

原稿时间推进 `504`、`48`、`16` 小时分别存于 `CH3-01`、`CH3-04`、`CH5-14` 的 `timeAdvanceHours`；转移表的建议延时保存在 `autoAdvanceMs`。它们是内容元数据，当前连续阅读引擎不模拟时钟、不等待定时转场，不因接入稿内延时而增加中途操作。

## 选择与状态

全部 57 个原选项按后继节点匹配 JSON 基线的已有选择 ID。初次接入时，CH3-03 第二项正文以原稿“先问清楚他为什么等了两天”为准，仍用 `ch3-03-ask`；不采用 JSON 旧标签“先问清楚列车为什么迟迟不来”。2026-09-08 C22-FIX获准仅在运行副本改为“先问清楚，这趟列车以前也这样不定时发车吗？”，避免在共同等待之前预支两天，选项ID、列车解释反馈和状态保留，见[全季接入映射C22-FIX](第十至第十三章内容接入映射.md#c22-fix-终审局部文案修订)。所有效果都是布尔旗标；玩家取向由选择写入，事实由结果节点进入时写入。

| 内容边界 | 实际写入或条件 |
| --- | --- |
| 父母问题 | `CH4-03/parents` 仅要求 `readParentsLetter = true`；本批内容不重复写该继承旗标 |
| 车站男孩 | `CH3-04-RESULT` 写入 `sawLittleDevilAtStation` |
| S 级揭示 | `CH3-06` 三个短结果均写入 `learnedSRank` |
| PPK 建立、掉落、拾取 | `CH4-07-RESULT` → `CH4-10` → `CH5-07-RESULT` |
| 第一枪与 M82A1 | `CH5-08` 击倒黑队狙击手后拾起 M82A1 |
| 后两枪 | `CH5-10` 先恺撒、后楚子航；所有异常反应选择合流，不改变战果 |
| 弗里嘉揭晓 | 仅 `CH5-11` 写入 `learnedAboutFriggaRounds`，此前可见文本不解释机制 |
| 狙击手身份与公开关系、奖励 | `CH5-13` 公共正文进入时写入三个事实旗标，不取决于是否追问 |
| 交换 | `CH5-07-RESULT` 写入 `exchangeWasNotCompleted`，不宣布交易完成 |
| 奖励待 3E | `CH5-14` 写入 `chapterFiveRewardPending3E` |

六个互斥选择组各写入所选取向的一项布尔旗标。原稿节点单向推进，不增加数值、字符串枚举或重复同义状态。

附录明确的两处条件措辞作为原问题的前缀实现，不扩写结果正文：`CH4-03/s-rank` 在 `questionedSRankBasis = false` 时显示原问题；为真时显示 `s-rank-recalled`，前缀为“继续追问 S 级：”。`CH4-05/ask` 在 `noticedLittleDevilEyes = false` 时显示原问题；为真时显示 `ask-recalled`，前缀为“认出同一双眼睛：”。两版使用同一组关键词、效果及后继，先过滤条件再匹配输入。

## 已批准的两处运行副本修正

2026-09-05 用户明确批准以下两处修正，仅在构建后的运行副本应用，原稿及逐字节快照保留。两处均已实装；源稿逐段对齐测试单独列出这两个批准差异，不放宽其他正文断言。

- CH5-06：“她在电影院里把你从人群中拽出来，在长途飞行中替你收拾狼狈，刚才又让你趴下”改为“她曾在那场告白之后出现在你面前，刚才又让你趴下”，兼容前两章分支并移除未发生的共同长途飞行回忆。
- CH5-09：“你明明不认识他们”改为“你明明不想向他们开枪”，保持路明非已认出校友楚子航的事实，突出抗拒射击的状态。

## 验证

`tests/chapters-three-five.test.ts` 独立构建三章测试故事，验证逐段正文、57 个原始选项标签、54 个短结果、信息揭晓时机、布尔类型、武器事实写入点、父母条件与全部 94 个节点可达。路径遍历只按后续条件使用的旗标合并等价状态，避免对无关历史取向做指数级枚举；实际选择与自动结果通过真实引擎执行。

另验证各选择节点的标签、关键词、自然短句、两意图歧义、宽词与父母选项关闭。首次运行暴露现有自由输入引擎会把合法关键词“第三项不用”的自然句当作否定指令拒绝；该问题已修复：先识别原稿中的完整关键词，再检查其余文字的否定语义；“我想第三项不用”可匹配，“我不想第三项不用”仍被拒绝，混合两个意图需澄清。30项程序测试全部通过。桌面及手机共10项浏览器测试通过，含三章独立测试、正式存档隔离、旧第二章末存档接续、连续走到第五章终点及刷新续读；构建与类型检查通过。独立内容及集成审查无阻断问题。

## 2026-09-05 CH3-03 文案修订

用户确认将蹭饭对白与“合伙申请”收尾两段替换为六段：芬格尔以八年大学经验推销自己，路明非接话并作内心吐槽。逐字采用已确认候选稿。原二十五美元、三项选择、短结果及旗标均保留。现有v0.5D Word六段黄色高亮，新增一条批注记录原因与旧文；Markdown沿用红字修订标记并加日期说明。未生成新的剧情参考版本。

运行版本为 `v0.5D-stage-two.2`，支持旧 `.1` 会话先按原文严格校验再重放，保留选择、旗标及检查点；旧v0.4B存档兼容仍保留。31项程序测试、类型检查及构建通过；浏览器专项验证六段新文可见、三个选项保留、修订标签不进入游戏。Word已渲染并查看受影响第4、5页，高亮与段落可读。
